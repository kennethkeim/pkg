{
  "name": "@kennethkeim/core",
  "version": "2.4.1",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "scripts": {
    "build": "tsup src/index.ts --format cjs,esm --dts",
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit --emitDeclarationOnly false",
    "sendit": "pnpm run build && pnpm run lint && pnpm typecheck && pnpm publish --access public",
    "test": "vitest --watch=false"
  },
  "keywords": [],
  "files": [
    "dist"
  ],
  "dependencies": {
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "tsup": "^8.0.1",
    "typescript": "^5.0.2",
    "vitest": "^2.1.9"
  }
}
